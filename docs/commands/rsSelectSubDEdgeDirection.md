# rsSelectSubDEdgeDirection · 选择 SubD 同向边

> 模块：SubD辅助 / 编辑工具

[← 返回命令目录](/commands/)

**功能**：选中 SubD 四边形区域中同方向的全部边

**调用**：在 Rhino 命令行输入 `rsSelectSubDEdgeDirection`（命令行交互）

**交互流程**：

1. 选择 SubD 物体
2. 点选一条控制网边
3. 自动选中全部同方向（U 或 V）的边

**参数**：

> 此命令无命令行数值参数，相关设置在窗口中调整。

**备注**：仅对四边形拓扑的 SubD 区域生效。
