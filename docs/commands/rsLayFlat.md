# rsLayFlat · 一键放平

> 模块：几何 / 对象变换

[← 返回命令目录](/commands/)

**功能**：将对象按指定面放平

**调用**：在 Rhino 命令行输入 `rsLayFlat`（命令行交互）

**交互流程**：

1. 选择对象上的目标面
2. 自动计算支撑点与面法向
3. 将对象旋转放平到该面

**参数**：

> 此命令无命令行数值参数，相关设置在窗口中调整。

**备注**：参照对象、引用对象与图块定义内的几何不可放平。
