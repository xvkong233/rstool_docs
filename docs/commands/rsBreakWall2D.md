# rsBreakWall2D · 打断二维墙体

> 模块：二维建筑 / 轴网与墙体

[← 返回命令目录](/commands/)

**功能**：在指定位置打断二维墙体

**调用**：在 Rhino 命令行输入 `rsBreakWall2D`（命令行交互）

**交互流程**：

1. 选择一面要打断的二维墙体
2. 在墙体上指定打断点
3. 墙体被打断成两段

**参数**：

> 此命令无命令行数值参数，相关设置在窗口中调整。

**备注**：需要打断的是 rsWall2D 生成的智能二维墙体。
