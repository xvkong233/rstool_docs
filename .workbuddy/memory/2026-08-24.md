# 2026-08-24 工作日志

- **rsReduceMeshByCurvature 文档同步源码更新**：用户让直接看源文件 `D:/GitHub/rstool/rstool/rsMesh/rsReduceMeshByCurvature.cs` 更新文档。源码比文档多一个参数 **保护角点 (Preserve corners, toggle, 默认 true)**：开启『保护裸边』时该选项自动置灰禁用；按 30° 边界转角阈值 (`BoundaryCornerAngleDegrees`) 识别轮廓转角顶点并在边折叠 (`TryCreateCandidate`) 时跳过，保留尖锐轮廓。另 UV 接缝/未焊接硬边两侧顶点同步锁定 (`FindProtectedSeamVertices`)。文档改动：在 params 数组『保护裸边』之后插入『保护角点』（顺序与源码 UI 一致：保护裸边→保护角点→保护硬折边）；notes 末尾追加「⚠️ 边界与接缝保护」段。Python 字符级 + 锚点 `    {\n     "zh": "保护硬折边",` 与 `迭代）"` 注入，json.loads 校验通过 (+321 B)，重生成 264 页 + touch config.ts；dev 死过一次已重启后台 (task k7MWvb)，无头 Chrome dump 命中 保护角点/Preserve corners/边界与接缝保护/30° 边界转角。用户说稍后贴新截图（该命令暂无 illustrations）。
