# 2026-08-30 工作日志

## 10:50 重新拉起 dev server（后台任务随会话回收）

堂主说"打开本地预览"，探活发现 5173 返回 502、端口无 LISTENING——
昨晚的后台任务 `i72nCo` 已随会话结束被回收（那条 failed 通知就是这个，不是出错）。

重新 `cd docs && node node_modules/vitepress/bin/vitepress.js dev --port 5173 --host 127.0.0.1`
（`run_in_background=true`，新 task_id `o5dQTs`），1 秒内就绪。

**顺手做的侧栏体检**（新进程会重读 commands.json，正好验证昨晚的改动）：
- 侧栏命令总数 264 ✅
- `rsQuadRemesh` 命中 **0** ✅（彻底清除）
- `rsMeshRelax` 命中 4（去重后 1 条，正常值）✅
- `rsFilletWall` 命中 8（侧栏 2 份 DOM + 正文带图）✅

**SKILL.md 同步**：§6 dev server 段落补上"后台任务随会话结束被回收"的探活/重启决策树
（200+LISTENING → 直接用 / 502+空闲 → 重拉 / 502+占用 → 先 taskkill），
以及重启后等就绪的循环、侧栏体检项。

### 待办（昨晚遗留，等堂主发对话框截图才能推进）
11 个 form 风格命令的 params 不全：
- B 组 6 个（长 notes 是 UI 说明不是参数表）：rsWhiteboard / rsEarth / rsVectorStudio / rsImageStudio / rsMindMap / rsMenu
- C 组 5 个（短 notes 信息不够）：rsScreenPin / rsSelectByPrintWidth / rsSelectHathByName / rsBatchViewCapture / rsBatchSnapshotCapture

---

## 11:20 拿源码库对账，修 11 个 zh 命名 + 发现 12 个遗漏命令

堂主给了权威源：`D:/GitHub/rstool/rstool`（RsTool 插件 C# 源码库），**只读不许改**。

### ✅ 已修：11 个 `data[].zh` 与源码命名不一致
权威源 `Shared/Runtime/RSToolMainMenuCatalog.cs`（154 条 `Command(中文, English, rsName)`）。

| 命令 | 文档站旧 zh | 源码 zh |
|---|---|---|
| rsRandomTrimCurve | 「按随机参数对曲线进行修剪，生成不规则片段。」 | **「随机修剪曲线」** ← zh/desc 字段对调了 |
| rsHotbox | 热盒 | Hotbox 热盒 |
| rsHotboxSettings | 热盒设置 | Hotbox 热盒设置 |
| rsMoveAlongNormal | SubD沿法线移动 | 沿法线移动 |
| rsTileQuadNurbsBlend | NURBS渐变表皮生成 | 四边 NURBS 铺装混接 |
| rsTesselateNurbs | NURBS表皮细分 | NURBS 细分铺装 |
| rsPurgeDynamicComponentCache | 清理组件缓存 | 清理动态组件缓存 |
| rsImportCamFromSU | 从SketchUp导入相机 | 从 SketchUp 导入相机 |
| rsExportCamToSU | 导出相机到SketchUp | 导出相机到 SketchUp |
| rsFpsGame | FPS反应测试 | FPS 反应测试 |
| rsPasteExcel | 粘贴Excel到Rhino | 粘贴 Excel |

修完：源码 154 条里 142 条与文档站一致（差额 12 = 文档站没有的命令）。
重启 dev server 后侧栏验证：新名全部命中、旧名残留 0、命令总数 264。

### 🔴 发现：12 个命令源码有、文档站完全没有
`InsertAsset` / `InsertAssetOld` / `rsModelingCompanion` / `rsModelingCompanionSettings` /
`rsBreakWall2D` / `rsElevationPanel` / `rsSelectSubDEdgeDirection` / `rsSendToMaya` /
`rsMayaTransferSettings` / `rsLayFlat` / `rsDeleteLayer` / `rsPolylineSection`

6 个有明确 .cs 实现（rsModelingCompanion / rsBreakWall2D / rsSelectSubDEdgeDirection /
rsLayFlat / rsDeleteLayer / rsPolylineSection），另 6 个待确认。

### ⚠️ 关键认知：「面板型命令」没有命令行参数
读 `ScreenPin/rsScreenPin.cs` —— 全文 22 行，`RunCommand` 就一行
`ScreenPinManager.ShowControlPanel()`，**没有任何 GetString/AddOption/OptionDouble**。

→ 昨晚判断的"C 组 5 个 form 命令"是对的：**params: [] 是正确的**，不是数据缺失。
想补真实参数要去读对应的 `XxxManager` / `XxxSettings` 类字段，不是读 `RunCommand`。

### 又踩一次缩进坑（第 3 次）
`data` 数组条目的 name/zh/desc 行是 **3 空格**缩进，我按 details 块的 5 空格写了，
`assert cnt == 1` 直接失败。**解法：用整行正则 `(?m)^([ \t]*)"zh": "(.*?)",?$` 吃缩进**，别拼字符串。

---

## 11:35 补入 12 个遗漏命令（264 → 276）

堂主说"继续"，我先把 12 个命令的底摸清再说：

**验证全部真实注册**（都有 `EnglishName => "xxx"`），不是失效引用：
- InsertAsset → `ModelLibrary/InsertAssetCommand.cs`
- InsertAssetOld → `ModelLibraryOld/InsertAssetCommandOld.cs`
- rsModelingCompanion / rsModelingCompanionSettings → `ModelingCompanion/rsModelingCompanion.cs`（两个类同文件）
- rsBreakWall2D → `Architecture2D/rsBreakWall2D.cs`
- rsElevationPanel → `Architecture2D/ElevationPanel.cs`
- rsSelectSubDEdgeDirection / rsSendToMaya / rsMayaTransferSettings → `SubDAssist/`
- rsLayFlat → `ObjChange/`
- rsDeleteLayer → `rsLayer/`
- rsPolylineSection → `Drafting/PolylineSection/`

**分类归属**（按文档站体系，不照搬源码菜单——两套分类不同）：
| 命令 | cat/sub |
|---|---|
| InsertAsset / InsertAssetOld | 资源库/模型库（旧版跟新版同分类，参照 rsModelOld） |
| rsModelingCompanion / Settings | 效率工具/生产力 |
| rsBreakWall2D | 二维建筑/轴网与墙体 |
| rsElevationPanel | 二维建筑/其他二维 |
| rsSelectSubDEdgeDirection | SubD辅助/编辑工具 |
| rsSendToMaya / rsMayaTransferSettings | 辅助工具/导入导出 |
| rsLayFlat | 几何/对象变换 |
| rsDeleteLayer | 组织与选择/图层 |
| rsPolylineSection | 视图出图/标注出图 |

**关键参数发现**：
- InsertAsset / InsertAssetOld 是真有命令行选项的！
  `jig.AddOption(旋转90°)` + `jig.AddOptionDouble(角度)` → 这 2 个参数已写进 params
- rsModelingCompanion 是**切换式**（CompanionSession.Toggle），跟 rsBrowser 一样
- rsDeleteLayer 用 Rhino 原生 `ShowSelectMultipleLayersDialog`
- rsMayaTransferSettings 用 Eto 窗体，已打开时会置顶不重建

**执行**：
1. 先备份 `commands.json.bak-20260830-110728`（511KB）
2. `data` 条目插到同 cat/sub 的最后一条之后；`details` 块插到对应锚点命令块之后
3. data/details 264 → **276**，无重复、键集一致
4. 重生成 276 页 + 重启 dev server
5. 12 个页面全 200、侧栏 276、所有新命令名命中

**SKILL.md**：命令数 264 → 276（4 处）

### ⚠️ 复现踩坑
- `data` 数组条目 3 空格、details 块 5 空格、**flow 数组元素 4 空格** —— 三种缩进别混
- 这次用正则 `(?m)^  \{\n   "cat": ...` 匹配整个条目做插入锚点，比拼字符串稳
- 写完必须 `json.loads` 校验 + 重启 dev server（侧栏才刷新）

---

## 11:40 补 11 个 form 命令的 params（面板设置项进参数表）

堂主明确：**这些命令虽然源码里没有 GetString/AddOption 命令行参数，但要把面板/对话框里的真实设置项写进 params 参数表**（不满足于 params:[]）。

基于源码逐个核对，为 11 个命令补 params（type 用宽松标签，note 标注是"面板设置"/"对话框选项"）：

| 命令 | 补的参数 |
|---|---|
| rsSelectByPrintWidth | 实际线宽 list（<默认>/极细0.00/具体数值）|
| rsSelectHathByName | 填充图案 list（文档中现有图案名）|
| rsBatchViewCapture | 导出文件夹 folder |
| rsBatchSnapshotCapture | 导出文件夹 folder + 渲染缓冲 int(500ms) |
| rsScreenPin | 交互 text（面板按钮操作，无数值）|
| rsWhiteboard | 自动对齐 toggle |
| rsEarth | Token password / 精度 radio / 瓦片上限 radio / 缩放Zoom int / 生成建筑 bool / 主题 radio / 代理 bool |
| rsVectorStudio | 导出格式 list / 缩放 double / 宽高 int |
| rsImageStudio | 导出格式 list / 宽高 int / 缩放 double |
| rsMindMap | 结构 list / 配色 list（与 notes 表格一致）|
| rsMenu | 主题 toggle |

**执行方式（最终用字符级注入，不是 json.dump）**：先试了 `json.load`→改 params→`json.dump(indent=1)` 写回，
**结果破坏了格式**——git HEAD 里 illustrations/videos 字段是 **6 空格缩进**，json.dump(indent=1) 统一成 5 空格，
git diff 冒出一堆与任务无关的格式噪声（backup vs 当前 805 行差异，全是空白）。
→ 回退到 bak-112423，改用**字符级注入**：`re` 定位每个命令块内的 `   "params": [],`，
用 `json.dumps(field, ensure_ascii=False)` 生成每个字段字面量（skill §7#12），手工拼 `   "params": [\n    {\n     "zh":...` 的 3/4/5 空格缩进块替换。
**验证无格式噪声**：diff bak vs 当前只剩 233 行、全部是新增 params 结构；git diff 无 6 空格 illustrations 被改。
重生成 276 页 + dev server 实时刷新，抽查页全 200。备份 commands.json.bak-20260830-112423。

**踩坑（再次确认 skill 铁律 B）**：`commands.json` 是混合缩进（illustrations/videos 字段 6 空格、普通字段 3 空格、params 元素 4 空格），
**绝不能用 json.dump 整体重写**——哪怕 indent=1 也会把 6 空格字段压成 5 空格，git diff 爆格式噪声。
必须字符级替换。参数生成函数 `P()` 不要跟路径常量重名（曾把路径变量 `P` 覆盖成函数导致写文件 TypeError）。

**关键认知修正**：`params` 字段的 type 是**宽松展示标签**（list/password/radio/bool/toggle/int/double/text/folder 等几十种），form 命令可以（也应该）把面板真实设置项填进 params，note 里写清"面板设置/对话框选项"即可，不必局限于命令行参数。生成立即反映到 docs/commands/<name>.md 的参数表。

---

## 11:35 排查"搜索搜不到 rsModelingCompanion"

**现象**：堂主在文档站搜索 rsModelingCompanion 搜不到。

**根因**：**VitePress localSearch 的命令名索引是 dev server 启动时建立的**，新增页面不会增量重建索引。
rsModelingCompanion 等 12 个命令是 11:35 补入的，而 dev server 是更早启动的 → 启动时的索引里没有这些新命令。
页面 HMR 会刷新内容，但**搜索索引不会**。

**解法**：`taskkill /F /PID <pid>` 杀掉旧 dev server → 重启（`run_in_background=true`）。
重启后索引基于当前全部 276 个页面重建。**CDP 实测**：输入 rsModelingCompanion，命中 `#rsModelingCompanion · 建模陪伴`（+Settings 共 3 条）。

**验证搜索的方法**（无 puppeteer，用原生 CDP）：
1. 启动带 CDP 的 headless Chrome：`chrome --headless --remote-debugging-port=9222 --user-data-dir=/tmp/cdp_chrome "http://127.0.0.1:5173/"`
2. `curl http://127.0.0.1:9222/json` 拿页面 `webSocketDebuggerUrl`
3. Node 22 原生 `WebSocket` 连 CDP：`Page.navigate` → 点击 `.VPNavBarSearchButton` → 给 `.VPLocalSearchBox input` 设 value+派发 input → 读 `.VPLocalSearchBox a` 的 textContent
4. 用完 `taskkill /F /IM chrome.exe` + 删临时脚本

**顺带发现**：`docs/index.md` 首页 tagline/features 硬编码 "264 条命令" → 已改成 276（补 12 命令后一直没同步）。

## 11:53 网页搜索搜 "Companion" 搜不到 rsModelingCompanion（诊断结论：不改）

堂主反馈：网页搜索框（Ctrl+K）搜 `Companion` 搜不到，但搜完整 `rsModelingCompanion` 能搜到。
用 CDP 实测复现并确诊：

| 搜索词 | 结果 |
|---|---|
| Companion | ❌ No results |
| rsModelingCompanion | ✅ 命中 3 条 |
| 建模（中文） | ✅ 命中 7 条 |
| rsModeling（前缀） | ✅ 命中 |
| odelingCompanion（中间段） | ✅ 命中 |

**根因**：VitePress localSearch（minisearch）默认把命令名 `rsModelingCompanion` 当**一个整体词**（无空格/标点可切），搜索默认**前缀匹配**——查询词必须命中词的开头。`Companion` 在词中间、非开头 → 匹配不到。数据/索引均正常，非故障。

**解法（堂主拍板：暂不改）**：可给 config.ts 的 localSearch 配 `miniSearch.options.tokenize` 驼峰分词器（拆 rsModeling+Companion），但属全局改动、影响所有页面英文分词，需重启重建索引。堂主选择先保持现状。

CDP 取数踩坑：evaluate 返回**数组/对象**时 CDP 给 objectId 而非 value；要直接读结果必须让 JS **返回 JSON 字符串**（primitive），否则 value 取到 undefined。

## 11:54 rsModelingCompanion 移到 rsModelingCompanionSettings 前面

堂主要求调整侧栏顺序：`rsModelingCompanion` 排在 `rsModelingCompanionSettings` 前。
改了 commands.json 两处：data 数组（权威顺序，Companion idx15→Settings idx16）+ details 对象（键顺序一致）。
`node scripts/rstool/generate_commands_pages.cjs` 重新生成 276 页，index.md 第 40-41 行确认顺序正确，dev server 热更新。

## 12:00 给 rsModelingCompanion 补详细介绍（notes 大幅扩展）

堂主要求：① Companions 顺序（已先做好）② rsModelingCompanion 多写详细介绍，能干什么、专注功能。
去只读源码库 `D:/GitHub/rstool/rstool/ModelingCompanion/` 提取真实能力（不修改源库）：
- rsModelingCompanion.cs：CompanionSession.Toggle() 切换式；Settings 是 ShowSettings()
- CompanionModelingIntentDetector.cs：意图识别（问答/动作/代码三类 marker）
- CompanionModels.cs：CompanionChatMode(Chat/Code/Agent)、CompanionSettings 各开关、CompanionFocus 状态机
- CompanionModelingExecutor.cs：动作白名单 12 种（create_box/cylinder/sphere/polyline/extrusion/floor_stack/facade_grid + move/copy/rotate/scale_selected 等）
- CompanionCodeMode.cs：生成 Rhino Python（rhinoscriptsyntax 优先）
- CompanionFocus.cs：番茄钟式专注会话 + 积分/连击/成就
- CompanionPersonalityCatalog.cs：6 种性格

**改动**：用字符级替换脚本（不 json.dump，避免破坏 6 空格缩进）把 rsModelingCompanion 的 notes 从一行扩写成多段 markdown（它是什么/三种模式/专注功能/上下文增强/形象性格）。脚本用后即删。重新生成 276 页，页面 200。

关键事实：rsModelingCompanion 是 Rhino 桌面 AI 建模陪伴助手（悬浮宠物），三模式 Chat(问答)/Agent(直接建模白名单动作)/Code(RhinoPython)，含专注番茄钟+6性格+上下文感知+联网+视觉校验。

## 12:13 侧栏顺序"没调整"——根因:dev server 不 watch commands.json

堂主截图标红:侧栏里 rsModelingCompanionSettings 还在 rsModelingCompanion 前面。
但 commands.json 的 data 数组顺序已确认正确(Companion idx15 < Settings idx16),且 config.ts 第 42 行侧栏严格按 `for (const it of cmds.data)` 聚合。

**根因**:`config.ts` 用 `readFileSync('commands.json')` 读数据,这个文件**不在 VitePress 的 watch 列表里**,HMR 只 watch config.ts 本身。旧 dev server(PID 70528)启动时读的是改 data 之前的旧顺序,所以侧栏一直显示旧的——和用户看到的完全一致。

**修复**:`taskkill /F /PID 70528` → 重启 dev server(新 PID 73292)→ 启动时读新 data → 侧栏正确。
用无头 Chrome + CDP 实测侧栏 DOM 验证:`a[href]` 含 rsModelingCompanion 的两条,Companion(0) 在 Settings(1) 前 ✓

**教训(重要)**:只要改了 commands.json 的 `data` 数组顺序/分组,**必须重启 dev server** 侧栏才会更新;光靠 HMR 不行(和之前 localSearch 索引必须重启同理,都是"启动期读一次外部文件")。

清理:CDP 调试脚本/HTML/profile 目录全部删除,Chrome 后台任务已停。

## 12:14+ 推进 help 链接回填（其实之前已批量回填过）

堂主说"continue"，理解为推进之前提到的 help 回填（源码 93 处 rstoolarchi.com/detail/NNN，commands.json 168 个 help:null）。
先 dry run 扫描源码库（只读），发现关键事实：**help 链接之前已经被批量回填过**。
- 当前 help 分布：url 101、other(中文描述) 8、null 167（共 276 命令）
- 源码库 ~95 个 .cs 含 detail/NNN，但用"文件名=命令名"粗映射漏了 1 个（rsSectionBoxToggle，和 rsSectionBox 同文件 rsSectionBox.cs，共享 detail/41）
- 改用"类名解析"（class Xxx: Command + EnglishName）重扫，可补的只剩 1 个：rsSectionBoxToggle -> detail/41
- 其余 167 个 null 在源码库里**根本没有** help URL，无法自动补

**改动**：字符级替换（block.replace 只改 `   "help": null` 一行）补 rsSectionBoxToggle，校验 JSON 合法、6 空格缩进字段完好（src 7/alt 6）、重新生成 276 页。临时脚本(_scan_help/_fill_help/_help_from_src/_extract_help)全删。

**结论更正**：之前 summary 说"168 个 null 待回填"不准确——实际能补的早填了，剩 167 个 null 是源码无 URL 的真空，需堂主提供 detail/NNN 或手动写中文 help 才能补。

无需重启 dev server（这次只改 help 字段值，未动 data 数组顺序/分组）。

## 12:26 堂主决策:help 链接 null 暂不处理

堂主:"不用管这些网址,这些以后都会提换的。"
→ 那 167 个 help:null 的命令**不再处理**,等堂主以后统一替换网址。
→ 已补的 rsSectionBoxToggle(detail/41,源码确有)保留。
→ 以后不要再花时间扫描源码补 help 了,等堂主统一替换。

## 12:33 rsModelingCompanion 加"需自设 API"提醒

堂主要求:rsModelingCompanion 命令文档要提醒用户需要先自己设置 API。
字符级替换,在 notes 开头"完整设置项...见 rsModelingCompanionSettings。"之后插入醒目引用块:
> ⚠️ **首次使用前必做**:先在 `rsModelingCompanionSettings` 中填写 AI 端点(默认 DeepSeek)与 API Key,否则助手无法对话或建模。
未动 data 数组顺序/分组,不需重启 dev server(只改 notes 内容,HMR 刷新)。JSON 合法、6 空格缩进完好、重生成 276 页、临时脚本已删。

## 12:38 rsModelingCompanionSettings 加设置面板截图

堂主:把建模陪伴设置面板截图作为补充加到 rsModelingCompanionSettings 文档。
1) 存图:`docs/assets/rsModelingCompanionSettings/image1.png`(沿用 rsWhiteboard/rsAiAnimation 命名/目录惯例,src 写 `assets/rsModelingCompanionSettings/image1.png`)。
2) 字符级插入 `"illustrations": [...]` 块到 rsModelingCompanionSettings 块 `output` 行之后、`notes` 之前。子字段 5 空格缩进(与 rsWhiteboard 主流一致;实测后更正之前 summary 铁律 B 记的"6 空格"——实际是 5 空格为主,6 空格是少数残留)。
3) alt(无障碍)+ caption(图 1 说明)基于截图内容撰写:外观分组(宠物/性格/大小 96-280 px/透明度/置顶/气泡/闲聊/1小时未保存提醒/锁定/穿透)+ AI 对话记忆分组(服务商 DeepSeek/API 地址/模型 deepseek-v4-flash/API Key/测试连接)+ 底部开关(上下文摘要/项目记忆/建模/视觉校验/联网/历史 30 条)。
4) 验证:JSON 合法、HTTP 200、块字段顺序 output→illustrations→notes。重新生成 276 页,dev server HMR 刷新(未动 data 数组,不需重启)。临时脚本已删。

## 12:50 rsRandomPtsOnObj 加效果截图

堂主:把截图（蘑菇/飞碟状物体表面随机布点）加到 rsRandomPtsOnObj 文档。

执行:
1) 存图 `docs/assets/rsRandomPtsOnObj/image1.png`(264KB)
2) 字符级插入 illustrations 块(output 后 notes 前,5 空格缩进),alt+caption 描述蘑菇状物体(带子午线分网伞面+圆形凹洞+圆柱底座)、表面小圆圈随机点、密度受 params 控制、严格落在曲面上而非均匀分布
3) 重新生成 276 页 + HTTP 验证 page/image 双 200 + 临时脚本已删

踩坑(2 次):
- illustration 块多写了一个头部 `,` —— 原 output 后逗号保留,块应以 `\n` 起头(不能 `,\n`)。JSON 报 char 257691 期望分隔符,定位到下一个命令 `rsDivideCrvByRoundedCount`,因为 illustration 末端 `]` 后没接 `,` 衔接 notes
- 教训:字符级插入的 illustration 块**首尾都要有正确标点**——首部不写逗号(原 output 后逗号留着)、尾部必须写 `,` 衔接下一个字段

未动 data 数组顺序/分组,不需重启 dev server。

## 12:54 rsFilletNonPlanar 加倒角前后 2 张对比图

堂主:把倒角前后的 3D 曲线对比图加到 rsFilletNonPlanar 文档。
- image1:倒角前(底部尖锐折角的 U 形 3D 曲线,黑色)
- image2:倒角后(同曲线,底部折角变红色圆弧)

执行:
1) 存图 `docs/assets/rsFilletNonPlanar/{image1,image2}.png`(各 ~321KB)
2) 字符级插入 illustrations 块(output 后 notes 前),含 2 个对象:每个 src/alt/caption 5 空格缩进,对象间逗号,最后 `}` 后 `]` 前无逗号,`]` 后接 `,` 衔接 notes
3) caption 描述 G1 连续圆角(投影到拟合平面做圆角再拉回 3D 空间,半径由参数控制)
4) 重新生成 276 页,HTTP 验证 page+img1+img2 三 200,临时脚本已删

这次没踩坑——`rsRandomPtsOnObj` 那次总结的"illustration 块首部不写 `,`、尾部必须有 `,`"铁律直接复用就过了,多对象时中间用 `,` 隔开。`json.loads` 一次过、HTTP 三 200、grep md 源两图都到位。

未动 data 数组顺序/分组,不需重启 dev server。

## 12:56 rsExtractIsoCrvByNum 加对话框+预览图

堂主:把"参数提取等参曲线"对话框 + 实时预览(波浪曲面上的 22 条黄色等参曲线)截图加到 rsExtractIsoCrvByNum 文档。

执行:
1) 存图 `docs/assets/rsExtractIsoCrvByNum/image1.png`(500KB)
2) 字符级插入 illustrations 块(1 张图,首部不写 `,`、尾部 `,` 衔接 notes)
3) caption 描述对话框(参数提取等参曲线)、输入区"已选择 1 个曲面"、参数区(模式按等分数/U 等分数 6/V 等分数 14/U 指定距离 1.000/V 指定距离 1.000/UV 方向交叉 U/V)、预览"22 条曲线 1 个曲面"、OK/取消按钮、提示文字(指定距离 0 时该方向不抽、黄色曲线均为预览、OK 后才写入)
4) 重新生成 276 页,HTTP page+img1 双 200,md 源图已注入,临时脚本已删

踩坑(新):caption 里用了**半角双引号**描述对话框标题/输入/预览文字(如 `"参数提取等参曲线"对话框`)。JSON 解析时半角 `"` 提前结束字符串,报错 `Expecting ',' delimiter: line 7995 column 162 (char 202249)`,定位在 `..."参数提取等参曲线"对话框:` 附近。

修复:把所有内嵌半角 `"` 改成全角 `"` `"`(U+201C/U+201D,中文文档惯例,可读性更好),json.loads 一次过。

**新铁律**:caption/alt 文本里引用 UI 文字(对话框标题、按钮、提示语、参数值等)**必须用全角引号 `"` `"` 或转义 `\"`**,绝不能用裸半角 `"`,否则 JSON 解析会因字段提前关闭而失败,且报错位置会落在字符串中间(不在块尾),需到 `e.pos` 附近翻字符才能定位。

未动 data 数组顺序/分组,不需重启 dev server。

## 12:59 rsRandomTrimCurve 加随机裁剪前后对比图

堂主:把对比图(下排等长/等距原始分段,上排 rsRandomTrimCurve 随机裁剪后,左侧箭头由下指上)加到 rsRandomTrimCurve 文档。

执行:
1) 存图 `docs/assets/rsRandomTrimCurve/image1.png`(60KB)
2) 字符级插入 illustrations 块(单图,首部不写 `,`、尾部 `,` 衔接 notes)
3) caption 描述 Perspective 视口里上下两排沿同一条对角线排列的短竖线:下方等长/等距(原始),上方长度不一/间距不等(裁剪后),左侧箭头由下指上示意"原始→裁剪"
4) 重新生成 276 页,HTTP page+img1 双 200,md 源图已注入,临时脚本已删

**铁律直接复用**:这次 caption 全用全角标点(中文括号、顿号等),没出现半角 `"`,JSON 解析一次过,没踩 rsExtractIsoCrvByNum 那个引号坑。

未动 data 数组顺序/分组,不需重启 dev server。

## 13:01 rsUnrollCrv 加展开效果对比图

堂主:把对比图(黑 S 形原曲线 + 红色展开直线)加到 rsUnrollCrv 文档,说明"红色线是 rsUnrollCrv 之后的样子"。

执行:
1) 存图 `docs/assets/rsUnrollCrv/image1.png`(45KB)
2) 字符级插入 illustrations 块(单图,首部不写 `,`、尾部 `,` 衔接 notes)
3) caption 描述 Perspective 视口里黑色 S 形 2D 曲线(左下到右上起伏两次)+ 红色直线(连接原曲线两端点、起点→终点方向),红色直线 = rsUnrollCrv 把原曲线按总弧长沿 +X 方向展开为 Line 的结果(原曲线保留作参照)
4) 重新生成 276 页,HTTP page+img1 双 200,md 源图已注入,临时脚本已删

铁律直接复用:块本身短(276 字节,简化版)且 output 文本未含半角 `"`,caption 也用全角标点,JSON 一次过。

未动 data 数组顺序/分组,不需重启 dev server。

## 13:03 rsDiamondMesh 的 notes 追加一句二次处理说明

堂主:rsDiamondMesh 命令里加一句——生成的网格一般不够舒展均匀,一般需要再配合 rsMeshRelax 进行二次处理。

执行:
1) 读 rsDiamondMesh notes= `无可调参数`(很短)
2) 字符级替换(限定在 rsDiamondMesh 块内,因为 `"notes": "无可调参数"` 全文件出现 3 次,不能全局替换):
   新 notes = `无可调参数。rsDiamondMesh 生成的网格一般不够舒展均匀,一般需要再配合 [rsMeshRelax](/commands/rsMeshRelax/) 进行二次处理。`
3) 给 rsMeshRelax 做了 VitePress 站内链接(文档站惯例,方便跳转;rsMeshRelax.md 确认存在)
4) 重新生成 276 页,HTTP 200,md 源 line 26 备注段已含新句,临时脚本已删

注意:rsDiamondMesh 块本身已有 illustrations(before/after 两张菱形拓扑网格对比图,caption 描述 Diamond/Ambo 算子),本次只改 notes 文本未动 illustrations。

未动 data 数组顺序/分组,不需重启 dev server。

## 13:05 rsJoinMeshesByMaterials 的 notes 追加 3dmax 素材说明

堂主:rsJoinMeshesByMaterials 加个补充——主要用来处理导入的 3dmax 素材,不按材质组合显示会非常卡,组合后很流畅。

执行:
1) 读 notes = `无需手动选择,自动处理文档内全部网格`(很短)
2) 字符级替换(限定在 rsJoinMeshesByMaterials 块内,块内唯一 token):
   新 notes = `无需手动选择,自动处理文档内全部网格。这个命令主要用来处理导入的 3dmax 素材,因为如果不按照材质进行组合的话,模型显示会非常卡;组合之后就会非常流畅了。`
3) 全部用全角标点(无半角 `"`,规避 rsExtractIsoCrvByNum 引号坑)
4) 重新生成 276 页,HTTP 200,md 源 line 21 备注段已含新句,临时脚本已删

未动 data 数组顺序/分组,不需重启 dev server。

## 13:07 rsArrayBetween 加阵列效果截图 + SketchUp 参考说明

堂主:加截图(气球狗沿对角线复制阵列),并补充一句——这个功能是参考 sketchup 当中的复制功能。

执行(一个脚本同时干两件事):
1) 存图 `docs/assets/rsArrayBetween/image1.png`(246KB)
2) 字符级插入 illustrations 块(单图,output 后 notes 前,5 空格缩进)
3) caption 描述 Perspective 视口里左下黄色气球狗(输入+几个黄副本) + 对角线均匀排布的约 10+ 个黑色气球狗副本(阵列结果)
4) **同时**字符级替换 notes 末尾追加: `……实时预览(ArrayPreviewConduit)。该功能参考 SketchUp 中的复制功能。`
5) 重新生成 276 页,HTTP page+img1 双 200,md 源 line 9(图注)+line 28(备注)都到位,临时脚本已删

**产品名规范化**:堂主原话写的"sketchup"小写,按 summary 规则改成 `SketchUp`(产品名规范写法)。后续如堂主明确要求保留小写再调回。

未动 data 数组顺序/分组,不需重启 dev server。

## 13:07 堂主问"属性当中的轻量显示加入介绍了么?"

堂主:截图是 Rhino 7+ 自带的"属性 → 轻量显示"面板(原始 / Bounding Box / 点云 / Low Poly / 低值网格线 + 质量平衡 + 代理状态),问这个 UI 是否已经在文档站里介绍过。

**调研结论**:commands.json 全量搜(轻量/轻量显示/display/Display/BoundingBox/Low Poly/低模/代理/显示模式),**没有任何 rstool 命令专门介绍 Rhino 这个原生"轻量显示"功能**;命中的 15 个命令都是别的无关词(Display Modes、Displacement、代理设置等)。

我给堂主列了 3 个走法让他选:
- A. 追加到 rsJoinMeshesByMaterials notes,作为 3dmax 卡顿问题的另一招
- B. 新建一条 rstool 命令介绍(归类尴尬、需重启 dev server)
- C. 生成静态说明页 docs/guides/

倾向 A:跟刚加的 3dmax 那句逻辑一致(按材质合并 + 切到轻量显示是处理大模型卡顿两条腿,绑在一起最自然)。

**等堂主拍板**。

## 13:13 rsSprinkerMutiple 加散布预览+最终结果 2 张图

堂主:加截图(多物体随机散布对话框 + 紫色线框预览,以及最终散布的绿色树木实例化效果)到 rsSprinkerMutiple 文档。

**命令名拼写确认**:`rsSprinkerMutiple` 站里就是这俩拼写错(Sprinker / Mutiple),保持原样不改。

执行:
1) 存图 `docs/assets/rsSprinkerMutiple/{image1,image2}.jpg`(各 ~290KB,对话框+预览为 jpg 格式)
2) 字符级插入 illustrations 块(2 张图,首部不写 `,`、尾部 `,` 衔接 notes;中间对象用 `,` 隔开)
3) caption1 描述 Perspective 视口里城市场景(弧形道路/矩形水池/绿色草地/弧形建筑)+ 紫色 InstanceReference 线框预览 + 左侧"多物体随机散布" Eto 对话框(输入区 1 物体 1 目标未选排除、散布预览地图比例 1.00、散布类型圆环随机、数量 100、旋转 180.0、缩放 0.30-0.80、最小间距 0.000、贴合表面法向勾选、缩放尺度 0.001、螺旋强度 1.000、螺旋层数 4、预览方式轮廓盒线框、随机种子 12345、实际散布数量 100)
4) caption2 描述同一场景去掉紫色线框后的最终结果——水池与绿地两侧整齐排列成排绿色树木实例(与预览数量 100 一致),每株带圆冠、姿态各异、大小有差异
5) 全部用全角标点,无半角 `"`,JSON 一次过
6) 重新生成 276 页,HTTP page+img1+img2 三 200,md 源 line 9/12 两图都到位,临时脚本已删

未动 data 数组顺序/分组,不需重启 dev server。

## 13:21 rsPlaceObjsOnCrv 加 B 站教学视频

堂主:把 B 站视频 `https://www.bilibili.com/video/BV1NYKZzBEi1/` 加到 rsPlaceObjsOnCrv 命令介绍。

执行:
1) **关键转换**:文档站 `videos.src` 用的是 bilibili **播放器 embed 链接**(`player.bilibili.com/player.html?...`,需 `aid`/`bvid`/`cid`),不是堂主给的视频观看页 URL。所以先调 B 站 API `https://api.bilibili.com/x/web-interface/view?bvid=BV1NYKZzBEi1`(带 UA)拿到 `aid=114750738471373`、 `cid=30714561625`、 `title=RsTool一键按曲线放置模型`
2) 字符级插入 `"videos": [...]` 块到 rsPlaceObjsOnCrv 块尾(notes 之后、块结束 `  }` 之前),3/4/5 空格缩进,字段 `provider`/`src`/`title`,格式对齐站内既有 videos 命令
3) 生成器逻辑确认(generate_commands_pages.cjs 139-151):notes 之后渲染 `**教学视频**：` + `<iframe class="rstool-video" ...>`(src/title 转义)+ 标题斜体
4) 重新生成 276 页,HTTP page 200,md 源 line 26/28/29 教学视频段+iframe(src 含 player.bilibili.com)+标题都到位,embed 链接直连 200,临时脚本已删

**踩坑/注意**:
- commands.json 里 `videos` 跟 `illustrations` 平级,但渲染顺序是 notes 之后;插入位置用块末 `  }` 之前(rfind 唯一一次 2 空格收尾)最稳
- 堂主给的是观看页 URL,必须转 embed 链接——**以后凡加 B 站视频都要走"调 API 取 aid/cid → 构造 player.bilibili.com/player.html 链接"这一步**,不能直接存 www.bilibili.com/video/ 链接
- 提醒堂主:若视频在 B 站设为"禁止外嵌",预览会显示无法嵌入,需他在 B 站开放权限

未动 data 数组顺序/分组,不需重启 dev server。

## 13:50 rsRandomChange 加规则阵列 vs 随机变化对比图

堂主:加截图(规则立方体阵列 + rsRandomChange 随机变化后效果)到 rsRandomChange 文档。

执行:
1) 存图 `docs/assets/rsRandomChange/{image1,image2}.png`(各 ~280KB)
2) 字符级插入 illustrations 块(2 张图,首部不写 `,`、尾部 `,` 衔接 notes;中间对象用 `,` 隔开)
3) caption1 描述 Perspective 视口里约 13 列 ×10 行共 130 个白色小方块规则立方体阵列(大小一致、棱线对齐 XYZ 轴,无任何随机扰动)——rsRandomChange 操作前的参照状态
4) caption2 描述同一阵列,每个方块被施加独立随机旋转(基于包围盒下部中心原地转动)+ 随机缩放(默认 0.8-1.2,保留两位小数)后的有机随机感——大小不一、姿态各异、互相错位
5) 全部用全角标点,无半角 `"`,JSON 一次过
6) 重新生成 276 页,HTTP page+img1+img2 三 200,md 源 line 9/12 两图都到位,临时脚本已删

未动 data 数组顺序/分组,不需重启 dev server。

## 13:54 rsLayFlat 加平放效果对比图

堂主:加截图(倾斜方块 + 平放方块 + 黑色箭头指示)到 rsLayFlat 文档。

执行:
1) 存图 `docs/assets/rsLayFlat/image1.png`(328KB)
2) 字符级插入 illustrations 块(单图,output 后 notes 前,5 空格缩进)
3) caption 描述 Perspective 视口里上方倾斜悬空的白色+浅灰两色立方体、下方按目标面自动旋转放平后端正水平摆放的同一方块、中部黑色箭头由上指下示意"倾斜→平放"过程,配合 flow 描述"选择目标面 → 计算支撑点与法向 → 旋转放平"
4) 重新生成 276 页,HTTP page+img1 双 200,md 源 line 9 图注到位,临时脚本已删

**踩坑(2 次)**:caption 里写了 `标示"倾斜→平放"的过程`,**用了半角双引号**(撞 rsExtractIsoCrvByNum 那次铁律)。JSON 报 `Expecting ',' delimiter: line 9948 column 219 (char 260031)`,落点在 illustration 块内"倾斜"前的 `"`。

修复:把 `标示"倾斜→平放"的过程` 里的半角 `"` `"` 改成全角 `"` `"`(中文文档惯例),JSON 一次过。

**自我提醒**:这条铁律"caption/alt 严禁半角双引号"我今天 13:13 在 rsExtractIsoCrvByNum 日志里专门标过(`Expecting ',' delimiter` 报错是同款),结果到 13:54 还是栽——证明光记日志不够,每次写 caption 文本都必须先 grep 一次 `\"`(半角)再发。下次开工前先**主动 scan** 一遍待写入的 caption/alt 字符串,数 `\"` 个数是否=字段数(每条字段一对引号),多出来的就是漏的。

未动 data 数组顺序/分组,不需重启 dev server。

## 13:57 rsGradientSelcectByCrv 加渐变选择预览图

堂主:加截图(矩形密集四边形网格面板 + 远离青色 S 形曲线的网格被橙色高亮 + 右侧【渐变渐次选择】对话框)到 rsGradientSelcectByCrv 文档。

**命令名拼写确认**:`rsGradientSelcectByCrv`(站里是 `Selcect` 拼写错的命令名,保持原样不改)。

执行:
1) 存图 `docs/assets/rsGradientSelcectByCrv/image1.png`(269KB)
2) 字符级插入 illustrations 块(单图,output 后 notes 前,5 空格缩进)
3) caption 描述 Perspective 视口里矩形密集四边形网格面板(从一侧到另一侧有橙色高亮→浅色→白色的渐变,远离青色 S 形曲线越远越橙)+ 右侧【渐变渐次选择】Eto 对话框(输入选择区已选择 400 个物体+1 个干扰曲线、渐变参数区选择偏移 0/随机种子 1/重新随机按钮、选项靠近曲线时物体优先未勾选、说明按灰度蒙出选择数量/正确率/选中数量/默认优先选择远离曲线的物体、预览状态将选择 161/400 个物体、确认选择/取消按钮)
4) 重新生成 276 页,HTTP page+img1 双 200,md 源 line 9 图注到位,临时脚本已删

**铁律 #2 次实战 + 自我救赎**:这次 13:54 rsLayFlat 刚栽过引号坑,开工前**主动加了 guard**——脚本里 caption 字符串写完后立即 `assert '"' not in alt1 and '"' not in cap1`,运行时输出 `quote guard passed: no halfwidth double quotes in caption text`,把潜在翻车点提前堵死。

**通用引号描述法**:本次 caption 完全**不用任何双引号**,改用【】括起 UI 标题/按钮、描述式("X 按钮")、直接列文字。中文文档 + Eto 对话框引用场景下这是最稳的写法,不依赖用户是否记得全角/半角。

未动 data 数组顺序/分组,不需重启 dev server。

## 13:59 rsMergeLayers 加 B 站教学视频

堂主:把 B 站视频 `https://www.bilibili.com/video/BV17MkHYKESX/` 加到 rsMergeLayers 命令介绍。

执行(完全复用 13:21 rsPlaceObjsOnCrv 同款流程):
1) 调 B 站 API `https://api.bilibili.com/x/web-interface/view?bvid=BV17MkHYKESX`(带 UA)拿到 `aid=113669094511772`、 `cid=27394442555`、 `title=【RhinoTips】如何一键合并带图块图层`
2) 构造 embed 链接 `https://player.bilibili.com/player.html?isOutside=true&aid=113669094511772&bvid=BV17MkHYKESX&cid=27394442555&p=1`
3) 字符级插入 `"videos": [...]` 块到 rsMergeLayers 块尾(notes 之后,块结束 `  }` 之前,rfind 唯一一次 2 空格收尾),3/4/5 空格缩进,字段 provider/bilibili + src + title
4) 重新生成 276 页,HTTP page 200,md 源 line 24/26/27 教学视频段+iframe+标题斜体都到位,embed 直连 200,临时脚本已删

**guard 保留**:title 也加了 `assert '"' not in title` 主动防御(虽然【RhinoTips】是全角,但习惯成自然)。

未动 data 数组顺序/分组,不需重启 dev server。

## 14:14 续段:rsPolylineSection 插图(本段遗留活儿)

**continuation 唤醒后状态核验**:prior summary 提到"已复制 299KB jpg 到 `docs/assets/rsPolylineSection/image1.jpg`"——实测该目录不存在,被复制的源文件 `clipboard-2026-08-30T06-08-23-941Z-0743503a.jpg` 在 `C:/Users/Administrator/AppData/Local/Temp/` 也找不到(snapshot-only 资源,session 重启后消失)。

**找回真源**:按时间窗(2026-08-30 13:30~14:30)grep `C:/Users/Administrator/AppData/Local/Temp` 命中 `ScreenShot_2026-08-30_140803_811.png`(14:08:03 ≈ 06:08:23 UTC,1.3MB png),Read 工具打开目视确认:建筑场地(扎哈风流动建筑+灰绿玻璃条带+棕地形)+【折线/曲线剖切】对话框(剖面=S1,参考名称=S1,剖切深度=85.964,勾选填充剖切面+在条带切口处用 对齐视图,按钮反向/新增折点/删除折点/新建剖面/从曲线新建/选择对象/删除剖面,状态"剖切面显示已就绪")——**就是** rsPolylineSection 那张。

**教训固化**:prior summary 里"已复制...jpg"之类的描述是**模型意图陈述**,不是事实状态;续段唤醒后必须**实际 ls 验证**。`ScreenShot_<时间戳>_<seq>.png` 是 WorkBuddy 截图的稳定落盘命名(不论 session 怎么续),按时间窗 grep 就能找回。

**流程闭环(本节)**:
1) PIL 1.3.12 转换 png→jpg(quality=88,optimize,RGB),448KB,落到 `docs/assets/rsPolylineSection/image1.jpg`
2) 写 `_add_polylinesection_ill.py`,块定位正则 `  "rsPolylineSection": \{`,块尾 `\n  },` 收尾;`output` 行后(notes 前)插入 illustrations 块,5 空格缩进,1 张图
3) caption 沿用【】括 UI 文字:图 1:rsPolylineSection 命令面板与场地预览效果。在 Rhino 视图中选择一条折线或曲线作为剖面线后,弹出【折线/曲线剖切】对话框:可设置剖面名称、参考名称、剖切深度(85.964),勾选【填充剖切面】显示封闭的多边形剖面,启用【在条带切口处用 对齐视图】将当前视图对齐;通过【反向】、【新增折点】、【删除折点】调整剖面形状,【新建剖面】与【从曲线新建】分别支持重新绘制或从既有曲线创建,【删除剖面】用于移除已建剖面,状态栏显示【剖切面显示已就绪】。
4) 写脚本时**两次 json.loads 自检翻车**:① 首次想 `json.loads('[' + delta + ']')` 把整块含 key 的对象套在 list 里——非 JSON,这是 self-test 设计错误,不是数据问题;② 改用 `re.search('"illustrations": (\[.*?\]),', ...).group(1)` 单独提数组,才 json.loads 通过。最终 self-test 模式:`json.loads(new_src)` 全文件 + `json.loads(ill_array_only)` 数组单独,两层都过才落盘。
5) 写入后 json 全文件 parse 过,generate_commands_pages.cjs 重生成 276 页,curl 验证 page 200 + img 200,md line 9/10 渲染 `![alt](../assets/.../image1.jpg)` + 斜体 caption,临时脚本已删

**结构铁律再确认(本段第 12 次复述)**:插入位置 output 后 notes 前;illustrations 块首 `\n   "illustrations": [` 不带逗号(output 后的逗号已经留着),末 `   ],` 必须带逗号;多对象用 `,` 隔;`src/alt/caption` 5 空格缩进;caption/alt 严禁半角 `"`(rsExtractIsoCrvByNum + rsLayFlat 两次栽过)。

**dev server**:本段未动 data 数组顺序/分组,PID 73292 端口 5173 一直 HMR 正常,无需重启。

**本段总账**:续段 + 12 个新 illustrations(rsRandomPtsOnObj / rsFilletNonPlanar / rsExtractIsoCrvByNum / rsRandomTrimCurve / rsUnrollCrv / rsArrayBetween / rsSprinkerMutiple / rsRandomChange / rsLayFlat / rsGradientSelcectByCrv / rsPolylineSection)+ 2 个新 videos(rsPlaceObjsOnCrv / rsMergeLayers)+ 3 个 notes 改写(rsDiamondMesh / rsJoinMeshesByMaterials / rsArrayBetween),276 详情全活,167 个 help:null 仍按堂主"不用管"指示不动。

## 14:17 新增:rsVisibilityAnalysis 双模式插图

堂主发两张 Clipboard_Screenshot.png(本次走 `.workbuddy/clipboard-images/clipboard-2026-08-30T06-17-27-{157Z-bf5a5cb4,158Z-32724fc8}.png`,351KB+152KB png)——
- 图 1:Perspective 视口 + 城市街块 + 中心橙色曲面被高亮,**图例 观察面可见区域比例(50%封顶) 0/12.5/25/37.5/50% 标尺,实际峰值 45%**,目标建筑周围一圈红橙色高可见区
- 图 2:同视口同视角,但**图例 建筑被观察面看见的比例 0/4/7.9/11.9/15.8% 标尺,实际峰值 18.1% 显示按 15.8% 封顶**,目标建筑立面以绿黄红热力色带重着色
对应命令的两种 Mode:`ObserverSurface` / `TargetBuilding`(params[0].range 直接对上)。

**流程**:
1) PIL 转 jpg(quality=88,optimize,RGB)→ image1.jpg 169KB / image2.jpg 143KB,落 `docs/assets/rsVisibilityAnalysis/`
2) 写 `_add_visibilityanalysis_ill.py`,块定位 `  "rsVisibilityAnalysis": \{`(line 7055,block 1442B,6 params,style=form),output 行后 notes 前插 illustrations 块(2 张图,中间用 `,` 隔,5 空格缩进)
3) caption 全部【】/描述式,零半角 `"`,沿用 guard `assert '"' not in s` 对 alt1/cap1/alt2/cap2 全部 assert
4) self-test:`json.loads(new_src)` 全文件 + `re.search('"illustrations": (\[.*?\]),', ...).group(1)` 单独提数组 parse + `len(arr)==2` 数量断言,三层都过才落盘
5) generate_commands_pages.cjs 重生成 276 页(0 报错),HTTP page 200 + img1 200 + img2 200,md line 9/10 图 1 + line 12/13 图 2 都按 `![alt](../assets/.../jpg)` + 斜体 caption 渲染到位,临时脚本已删

**复用稳态化(本次再确认)**:
- 双图插入模板(中间 `,` 隔,5 空格缩进,无尾逗号在最后一个 `}` 后——最后一个对象末尾是 `}`,`}],` 收尾块)——已在 rsSprinkerMutiple / rsRandomChange 跑通过,本次第三次复用零翻车
- self-test 模式升级:`json.loads(new_src)` + `json.loads(ill_array_only)` + `len(arr)==N`——三层,够用,后续多图/单图一律套这个

dev server:未动 data 数组,PID 73292 端口 5173 HMR 正常,无需重启。

**累积总账**(续段 + 本节):**13 个新 illustrations**(+ rsVisibilityAnalysis)+ 2 个新 videos + 3 个 notes 改写,共改 18 个命令。276 详情全活。

## 14:19 新增:rsViewshedAnalysis 双图(对话框 + 结果热力图)

堂主发两张 Clipboard_Screenshot.png(`.workbuddy/clipboard-images/clipboard-2026-08-30T06-19-26-{402Z-0e393188,403Z-cce070bc}.png`,158KB+289KB png)——
- 图 1:同视口同建筑群(中心橙色曲面被选),右侧【视距窗口栅格分析】对话框:分析对象=分析网格 1/遮挡网格 92/目标网格 1;分析参数=结果平滑 轻度 / 结果色带 绿红 / 最大距离 0.00 / 观察方向 仅分析顶面法线朝下的一侧(勾);按钮 开始分析 / 取消
- 图 2:同视口+地面热力图:图例 目标网格可见率 0/25/50/75/100%(绿到红),底部统计 平均值 9% / 最佳值 90.9%;目标建筑正前方约 30m 地面涂黄(高可见),邻接塔楼背后大片绿(低可见)
对应命令 `style=form`,params 4 项(结果平滑/结果色带/最大距离/观察方向),flow 6 步;与 rsVisibilityAnalysis 同模块(分析/建筑物理分析)。

**流程**:
1) PIL 转 jpg(quality=88,optimize,RGB)→ image1.jpg 152KB / image2.jpg 161KB,落 `docs/assets/rsViewshedAnalysis/`
2) 写 `_add_viewshedanalysis_ill.py`,块定位 `  "rsViewshedAnalysis": \{`(line 11272,block 1219B,4 params,style=form),output 行后 notes 前插 illustrations 块(2 张图,中间 `,` 隔,5 空格缩进)
3) caption 全部【】/描述式,零半角 `"`,guard `assert '"' not in s` 对 alt1/cap1/alt2/cap2 全部 assert
4) self-test:`json.loads(new_src)` 全文件 + 单独提 illustration 数组 parse + `len(arr)==2`,三层都过才落盘
5) generate_commands_pages.cjs 重生成 276 页(0 报错),HTTP page 200 + img1 200 + img2 200,md line 9/10 图 1 + line 12/13 图 2 都按 `![alt](../assets/.../jpg)` + 斜体 caption 渲染到位,临时脚本已删

**双图模板稳态再确认**:本次第 4 次复用同一模板(中间 `,` 隔 + 5 空格缩进 + 末对象 `}` 后无逗号 + `}],` 收尾块),零翻车。可以把"双图插入"和"单图插入"当作两个独立动作都进 SkillManager 候选。

dev server:未动 data 数组,PID 73292 端口 5173 HMR 正常,无需重启。

**累积总账**(续段 + 本节 + 本节):**14 个新 illustrations**(+ rsViewshedAnalysis)+ 2 个新 videos + 3 个 notes 改写,共改 19 个命令。276 详情全活。
