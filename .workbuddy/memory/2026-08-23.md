# 2026-08-23 工作日志

- **Linked C# 加 B 站视频补充**：`details['Linked C#']` 原无 `videos` 字段（块结构 flow/params/illustrations/output/notes，notes 为块最后字段、结尾无逗号）。新增 `videos` 数组 1 条：标题「RsTool · 我该怎么用AI帮我在Grasshopper中提效？（B 站）」，bvid=BV1gB826ZERJ / aid=117139411310006 / cid=41164472955（沙箱 curl 外网受限，改用 WebFetch 直连 bilibili API 取元数据）。注入脚本字符级定位 notes 边界、在闭合 `"` 后补 `,` 再插 videos，JSON 校验通过；generator 重生成 264 页；无头 Chrome dump 命中 `<iframe class="rstool-video" src="...bilibili...">` + 标题 + 「教学视频」段。

- **工具注意**：本工作区 Grep 工具偶发漏检（搜 `Linked C#`/`rsSectionBox`/`"videos"` 均报 no matches，但文件里真实存在，Bash `grep` 可正常命中）。诊断文件状态时用 Bash grep 兜底，别被 Grep 工具空结果误导。

- **git 状态变化**：commands.json 现在 `git status` 干净（之前各命令的 illustrations/videos/notes 改动看来已进 commit），不再是"未提交"状态。

- 工作区未提交的本地 commit `14ad3455` 仍可能存在；push 仍受 GCM 写操作卡死限制，待堂主侧处理。

- **rsEscalator 替换示意图**：用户说"更新了一下，替换一下图片"。新截图内容结构（Eto 弹窗 5 参数 + Rhino 视口扶梯模型 + 黄色引导线）与旧图基本一致，alt/caption 仍准确。只动了 `docs/assets/rsEscalator/image1.png` 二进制（138 742 B），commands.json / md 未动；路径未变所以 vitepress 热重载直接出新图，无头 Chrome dump 仍命中 `/assets/rsEscalator/image1.png`，dev HTTP 200 + Content-Length 138 742 验证通过。

- **rsZigZagRamp 替换示意图**：用户说"更新了一下工作流，替换一下示意图"。截图右侧露出 Eto 『折线坡道』弹窗（嵌入 1 条路径 + 3 参数：坡道宽度 / 栏杆高度 / 生成方向；预览已更新 + 生成/取消），原 alt/caption 没写到弹窗，趁机收紧：二进制 `docs/assets/rsZigZagRamp/image1.png` 直接覆盖（237 474 → 271 490 B），commands.json 里 illustration 对象的 alt/caption 用 Python 字符级正则 + `json.dumps` 重写（+100 B），重生成 264 页 + touch config.ts，dev 200、新 image1.png 271 490 B、无头 Chrome dump 命中新 alt + 新 caption + 新 src。今后"换图 + alt/caption 微调"走这条捷径。
