# 2026-08-28 工作日志

## git 历史冲突解决（rstool_docs）—— 最终结论
- 现象：GitHub Desktop "Pull origin" 报 `Unable to merge unrelated histories`。根因：本地 main 与远程 origin/main **无共同祖先**（两边从不同 init 来源，远程 15 commit 全在 2026-08-27 一天，本地 2880 commit 从含 VitePress 上游历史的快照 init）。
- **关键纠偏（重要）**：之前误判"本地是远程超集、内容缺失在本地"。实际用 Python 数 `git ls-tree` 确认：origin/main 已含**全部 265 个 docs/commands/*.md** 以及所有 commands.json 数据（rsAiAnimation 视频、保护角点、rsSectionBoxToggle 等）。即**这周的文档工作早在前那次 `git pull`（merge commit 67115607）时就已进 origin/main**。unrelated histories 只是历史不连通，**内容本身两边完全一致**（tree hash 同为 f8211bd0）。
- 操作：堂主决策"以远程为基底重做"，先 `git branch main-backup-20260828`（指向 67115607），再 `git reset --hard origin/main` → 本地 HEAD=`0fec6e68`，ahead/behind `0/0`，本地与远程完全同步。
- **结果：没有需要"补"或"提交"的内容**。git status 干净（仅 `.workbuddy/memory/2026-08-28.md` 这个内部文件 untracked，不进仓库）。generator 重生成 265 页，内容与已 committed 完全一致 → 无 diff。
- backup branch `main-backup-20260828`：因 tree == origin/main，**无额外兜底价值**，可保留可删。
- 下一步：堂主可直接在 GitHub Desktop "Push origin"（FF，no-op 或推送新改动）；历史已连通，不再报 unrelated histories。
- **踩坑**：Git Bash 下 `git ls-tree origin/main --name-only | grep -c "docs/commands/.*\.md"` 会**漏检误报 0**（Grep 工具也有类似漏检）。涉及 git tree 计数务必用 Python `subprocess` 直接数，别信 shell grep。

## 新增 rsDisplacementToMesh 命令文档
- 源文件 `D:\GitHub\rstool\rstool\rsMesh\rsDisplacementToMesh.cs`（RSTool.rsMesh 命名空间）。功能：把 Rhino 对象的**置换(displacement)/凹凸(bump)贴图烘焙成真实网格几何**，带 Eto「置换贴图转真实网格」对话框 + 橙色半透明实时预览(DisplacementPreviewConduit)，可取消的受控最终生成，输出对象命名 `<原名或 Displaced>_Displaced` 并写 UserString(RSTool.Type=DisplacementMesh 等)。
- 操作：读源码提取参数，字符级插入 `scripts/rstool/commands.json`——`data` 列表（紧跟 rsReduceMeshByCurvature 之后，cat=几何/sub=网格，2 空格缩进）+ `details` 块（18 params / 6 flow / output / notes，style=form, license=true）。重生成 266 页 + touch config.ts，dev 200，无头 Chrome 命中全部 9 关键词。
- **踩坑（重要）**：commands.json 文件结构是 `data` 在前（**2 空格**缩进：`  {`/`   "cat"`/`  },`）、`details` 在后（**4 空格**缩进：`  "rsX": {`/`    "license"`/`  },`）。data 项插入锚点必须用 `"  },"`（2 空格）搜结束符；若误用 `"    },"`（4 空格）会跨过整个 data 数组把新项误插进 details 区。第一次就用错锚点，把 data 项插到 details 的 rsAiAnimation params 里（1995 行），导致 data count 不变、页面不生成；已 `git checkout -- scripts/rstool/commands.json` 回滚基线后改锚点重跑才对。**修正后**：data 锚点用 `"name": "rsReduceMeshByCurvature",` + 其后首个 `"  },"`。
- 该命令**暂无示意图**（堂主未发截图）；如需补图，贴截图后加 `illustrations` + 拷 `docs/assets/rsDisplacementToMesh/image1.png`。

## rsCurtainWall2D 补图
- 堂主贴图（截图场景：Rhino 视口一段已生成的 L 形 2D 幕墙——黑色线段 + 等间距朝外短斜线竖梃；右上角第二段正在用橙色橡皮线预览延伸，标 7.0066 m，十字光标在端点）。原命令在 commands.json 存在（details 有 flow 3 步 / output / notes「默认按 1200/40/200/30mm 适配模型单位」27 字 / 6 params），但 `illustrations:[]` 空。
- 操作：建 `docs/assets/rsCurtainWall2D/` + cp 截图改 png 后缀 (45 864 B)；用 Python 字符级注入 `"illustrations":[{src,alt,caption}]` 到 `"notes": "默认按 1200/40/200/30mm 适配模型单位"` 之后（用 anchor `'"notes": "..."'` 唯一性 + 2 空格缩进对齐顶层键）。`json.dumps(ensure_ascii=False)` 包 alt/caption。delta +352 B。重生成 + touch config，dev `commands/rscurtainwall2d` 200，HEAD image1.png 200。无头 Chrome dump 命中 `image1.png` + 新 alt「2D 幕墙延伸预览」+ 新 caption「沿中心线延伸幕墙时」+ length 关键词「7.0066 m」（图片里出现过的字符串也命中，确认 dev 真渲染了新资产，不是缓存）。
- 临时脚本 `probe_block.py` + `insert_curtainillus.py` 已删。

## CanvasTransparent 补图（部分完成）
- 堂主贴新截图想"补充"。但新图 `clipboard-2026-08-28T04-04-17-446Z-91d6012e.png` **始终没落盘**（系统提示"当前模型不支持图片"，且文件实际未写入，等约 7s 仍 STILL MISSING）——先请堂主重发。
- **顺手修了一个历史孤儿 bug**：`assets/CanvasTransparent/image1.png`（412 017 B，8-21 创建，且已被 git 跟踪 `git ls-files` 有）却**从未被 commands.json 引用**（origin/main 里 `CanvasTransparent.details` 无 `illustrations` 键，只有 icon/license/help/style/flow/params/notes）。用 Python 字符级注入 `"illustrations":[{src:"assets/CanvasTransparent/image1.png", alt:"CanvasTransparent · Grasshopper 画布背景透明化，透出后方 Rhino 视口", caption:"...实时对照 Rhino 几何"}]`（anchor 用 `"notes": "该运算器在 Grasshopper 画布中运行` 前缀，扫描到 notes 字符串结束引号再插，2 空格缩进对齐顶层键）。delta +269 B。重生成 + touch config，dev 200，无头 Chrome 命中 `image1.png` + alt「Grasshopper 画布背景透明化」+「透出后方 Rhino 视口」→ 旧图现在能显示了。
- 临时脚本 `fix_canvas_illus.py` 已删。
- 待办：堂主重发新截图后，拷为 `image2.png` 并追加第 2 条 illustration。

## CanvasTransparent 图替换（image1）
- 堂主新发截图落在 `.workbuddy/blobs/b5/b5c514...png`（228 052 B，8-28 12:24）。命令："用我新发的替换掉现在的图"——指 image1 整体替换，不是新增 image2。
- 操作：`cp` blob → `docs/assets/CanvasTransparent/image1.png` 覆盖（原 412 017 B → 新 228 052 B，视觉一致，只是更新了一版更小尺寸的截图）。`illustrations` 仍只有 image1 一条，无需改 commands.json。generator 不必重跑（src 路径未变）。dev HEAD `/assets/CanvasTransparent/image1.png` 200 / Content-Length 228 052 / Last-Modified 04:25:53，无头 Chrome 命中 `<img src="/assets/CanvasTransparent/image1.png" width="2560" height="1392">` + alt/caption。

## CanvasTransparent 替换反复（新图再次未落盘）
- 用户发「用这张图提换」附 `Clipboard_Screenshot-1.png`（12:47）。但**该图再次未落盘**（无 `<image_local_path>`、clipboard-images 与 blobs 内 12:46 后无新 png）——同 23 号那次「新截图未落盘」坑。
- 上一轮误把用户整屏 docs 页截图（452 051 B，`clipboard-2026-08-28T04-30-33-489Z-abd3de17.png`，含浏览器栏+侧栏+内嵌老图，递归截图）当替换图 cp 进 image1.png。已**回退**到干净版（blob `b5c514...` 228 052 B，犀流堂 logo 视口+GH 画布的纯组件截图）。dev HEAD Content-Length 228 052，Last-Modified 04:50:14。
- 现状：image1.png = 干净 228 052 B 版；待用户重发正确的 `Clipboard_Screenshot-1.png`（真正的 CanvasTransparent 组件/效果截图）后 cp 覆盖即可。
- **判断**：用户上一轮 @image#1(Clipboard_Screenshot.png) 是"还没替换"证据图、@image#2(Clipboard_Screenshot-1.png) 才是真替换图；系统只把 #1(整屏递归) 当 `image_local_path` 给了模型，#2 始终没真正投递。本轮用户单独重发 #2 仍未落盘。

## CanvasTransparent 替换成功（最终版）
- 用户改用本地路径发图（`E:\Data\WXWork\1688850659137053\Cache\Image\2026-08\65b99f09-8972-4950-8690-66c875b1c79d.png`，880 496 B，3840×1809）——绕过了剪贴板投递问题。`@image_local_path` 直接给了文件系统路径，模型能读到。
- 图内容：Grasshopper 画布多组件（Divide/Project/Offset/Create plane/Evaluate columns/Toggle/Width 等）+ 暗色半透明背景透出 Rhino 视口里的两个漫画人物——**就是 CanvasTransparent 启用后的实际效果**，比之前犀流堂 logo 视口那张更直观展示"画布透明透出 Rhino"。
- 操作：`cp` 覆盖 image1.png（原 228 052 B → 新 880 496 B）。`illustrations` 仍只 image1 一条，commands.json 无需改，generator 不必重跑。dev HEAD Content-Length 880 496 / Last-Modified 04:32:12，无头 Chrome 渲染新图正常，标题"CanvasTransparent"、模块"GH组件 / Util"、caption 完整显示。
- **经验（重要）**：发图走"先存本地文件 → 给路径"是最稳的方式，平台对剪贴板直发的图经常不写入文件（连发 3 张都丢失了 2 张）。以后替换 image*.png 优先让用户用本地路径。
